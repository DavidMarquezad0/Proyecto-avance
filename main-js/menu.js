function toogleMenu(){
    const menu = document.getElementById("menu");
    menu.classList.toggle("menu--is-open");
}

function toogleClose(){
    const menu = document.getElementById("menu");
    menu.classList.toggle("menu--is-open");
}
